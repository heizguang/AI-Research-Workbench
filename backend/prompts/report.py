"""
报告智能体提示词
ReportAgent 使用的所有提示词模板
"""


# ── 报告生成 ──────────────────────────────────────────────

def generate_from_ai(topic: str, format: str) -> str:
    """无外部内容时生成报告（LLM 可自行决定是否搜索）"""
    from datetime import datetime
    today = datetime.now().strftime("%Y-%m-%d")
    return f"""请为以下主题生成一份详细的报告：

主题：{topic}

## ⚠️ 重要约束
- **当前日期是 {today}**
- **时间范围：默认聚焦最近 1 年的信息**。不要引用超过 1 年的旧数据，除非用户明确要求历史分析
- **优先搜索**：如果对主题信息不确定，先调用 web_search 获取最新资料，不要凭训练数据生成
- **禁止编造**：不凭空编造任何数据、日期、URL

报告应该包含：
1. 摘要/概述
2. 背景介绍
3. 主要内容
4. 分析与讨论
5. 结论与建议
6. 参考文献（如有）

## 参考文献输出要求
在报告正文结束之后，**必须**输出一段结构化的参考文献 JSON，用以下标记包裹：

__REFS_START__
[
  {{"title": "文献标题", "author": "作者", "year": "年份", "url": "可访问的URL或空字符串"}},
  {{"title": "文献标题", "author": "作者", "year": "年份", "url": "可访问的URL或空字符串"}}
]
__REFS_END__

- 每条参考文献必须包含 title 字段，其他字段（author、year、url）尽量填写
- url 字段：只填写你确定可访问的真实链接，**不确定就留空字符串""**，严禁编造
- 如果没有参考文献，输出空数组 []

请使用{format}格式输出。"""


def generate_from_document(content: str, topic: str, format: str) -> str:
    """文档模式：基于上传文档生成报告"""
    return f"""基于以下文档内容，为"{topic}"生成一份报告：

文档内容：
{content}

报告应该包含：
1. 摘要/概述
2. 文档关键信息
3. 分析与讨论
4. 结论与建议

请使用{format}格式输出。"""


def generate_from_search(content: str, topic: str, format: str) -> str:
    """基于外部内容生成报告（带引用规范）"""
    return f"""# Role: 专业行业研究分析师

## Task
请严格基于下方提供的【搜索结果】，为"{topic}"撰写一份深度研究报告。

## ⚠️ 输出规范
- 直接输出报告正文，以标题开头（如 `# {topic}`）
- 不要添加任何开场白、自我介绍或说明性文字（如"好的，我将为您…"、"作为一名分析师…"）
- 不要在报告前后添加任何对话性内容

## ️ 核心约束（必须遵守）
1. **严格忠于搜索结果**：报告中的所有事实、数据、时间点、版本信息必须且只能来源于【搜索结果】。搜索结果未覆盖的时间段、版本、事件，直接在报告中说明"当前搜索结果中未包含该信息"，**严禁用模型训练数据填充**。切勿编造任何数据。
2. **内容完整性**：请充分利用搜索结果中的所有信息，确保报告覆盖全面。
3. **正文禁止任何来源标注**：正文中不得出现任何形式的来源标注，包括但不限于 [来源X]、(来源X)、来源X、[来源 X]、(来源 X)、[1]、(1) 等。正文直接陈述事实和数据即可，不要在正文中提及任何来源编号。
   - ❌ 错误示例：YOLO26在COCO上达到了40.9-57.5 mAP（来源 10）。
   - ❌ 错误示例：YOLO26在COCO上达到了40.9-57.5 mAP[来源 10]。
   - ✅ 正确示例：YOLO26在COCO上达到了40.9-57.5 mAP。

##  报告结构要求
请严格按照以下大纲撰写，并使用 **{format}** 格式输出：

##  Markdown 格式规范
- **表格**：使用标准 Markdown 表格语法，表头与分隔符必须对齐，每列用 `|` 分隔，分隔行使用 `---`（不需要冒号）。
- **有数据才出表格**：搜索结果中有具体数值/参数的内容才用表格呈现；没有数据就不需要表格，用文字叙述即可。
- 表格前后各空一行，确保正确渲染
- 数据来源在表格下方单独一行，用斜体标注：*数据来源：XXX*

##  报告章节
请根据主题特点和搜索结果内容，自行设计最合适的报告结构（4-6个章节）。不要使用固定的通用章节标题，要根据主题定制。
- 每个章节必须有实质性内容，不要空洞
- 搜索结果中有具体数据的版本、型号必须在报告中体现；没有数据的可以用一两句话概述，不必强行展开
- **不得遗漏搜索结果中的对象**：搜索结果中提到的所有版本、产品、方案等对象，每个都必须在报告中有所体现（至少一句话）；没有具体数据的可用一句话概述，不必强行展开
- **注意命名不同的版本**：如果搜索结果中提到了与主线版本命名规则不同的最新版本（如 YOLO26/YOLOv26、iPhone SE、Windows Server 等），必须单独列出并详细描述，不要因为命名风格不同而遗漏
- 如果搜索结果涉及多个对象的对比，优先用表格呈现有数据的对象
- **必须有汇总对比表**：如果报告中逐个描述了多个对象（如多个版本、多个产品、多个方案），且它们有可比的量化指标，必须在报告中提供一个汇总对比表（优先放在正文前面），不要只在正文中分散描述
- **数据表格必须具体**：如果搜索结果中提到了具体版本的数值数据（如 mAP、FPS、参数量等），必须以表格形式逐版本列出，不要用一个汇总范围代替
- **跨文章汇总数据**：如果同一维度的数据分散在多篇搜索结果中（如多个产品的销量、多个版本的性能指标、多个地区的市场数据），应跨文章汇总到同一个表格中，不要因为单篇文章没有完整数据就不做汇总表
- **优先选择权威数据**：如果搜索结果中包含多种口径或场景的数据，优先选择行业公认的标准指标或权威来源作为主要对比依据，其他数据可作为补充

##  参考来源规范
在报告最末尾添加 `## 参考来源` 章节。
- **列出相关搜索结果**：搜索结果已经过相关性过滤和去重，请列出所有搜索结果作为参考来源。
- 每个来源单独一行，格式严格如下：
  1. **文章标题** — [查看原文](URL链接)
  2. **文章标题** — [查看原文](URL链接)
- **⚠️ 禁止编造 URL**：只使用搜索结果中提供的原始链接，**严禁自行拼接、推测或编造任何 URL**。如果搜索结果中没有提供链接，只写标题即可，格式为：`1. **文章标题**`
- **去重规则**：相同的URL链接在参考来源中只能出现一次。
- 示例（假设搜索结果有3条）：
  ## 参考来源
  1. **Ultralytics YOLO26 | Ultralytics** — [查看原文](https://docs.ultralytics.com/zh/models/yolo26)
  2. **A Review of YOLO Family from YOLOv1 to YOLO26** — [查看原文](https://doi.org/10.20944/preprints202602.1844.v1)
  3. **目标检测之YOLO系列的发展历程概述** — [查看原文](https://example.com/article)

## 参考文献输出要求
在 `## 参考来源` 章节之后，**必须**输出一段结构化的参考文献 JSON，用以下标记包裹：

__REFS_START__
[
  {{"title": "文献标题", "author": "作者", "year": "年份", "url": "搜索结果中的原始URL或空字符串"}},
  {{"title": "文献标题", "author": "作者", "year": "年份", "url": "搜索结果中的原始URL或空字符串"}}
]
__REFS_END__

- 每条参考文献必须包含 title 字段，其他字段（author、year、url）尽量填写
- url 字段：只使用搜索结果中提供的原始链接，**严禁编造**，不确定就留空字符串""
- 如果没有参考文献，输出空数组 []

---
##  搜索结果（完整内容）：
{content}
"""


# ── 报告修改 ──────────────────────────────────────────────

def modify_report(report: str, modifications: str, format: str, search_context: str = "") -> str:
    """根据修改意见修改报告"""
    context_section = ""
    if search_context:
        context_section = f"""

原始搜索结果（修改时必须基于这些数据，不得杜撰）：
{search_context}"""

    return f"""请根据以下修改意见修改报告：
{context_section}

原始报告：
{report}

修改意见：
{modifications}

要求：
1. **禁止编造**：所有数据必须来自原始报告或补充搜索结果，不得自行编造、推测或使用训练数据补充。如果信息不足，直接说明"搜索结果中未包含此信息"，不要用推测性描述填充
2. **信息不足时的处理**：如果修改意见需要补充大量新信息（如详细列举子版本、补充具体数据等），而搜索结果中没有这些信息，请在回复中明确告知用户："此修改需要更多搜索数据支撑，建议使用「重新生成功能」并指定重点需求"，然后基于现有信息完成力所能及的修改
3. 保持数据一致性，同一数据不要前后矛盾
4. 正文中不要出现 [来源X] 等标注，正文直接陈述内容
5. 在报告末尾添加"## 参考来源"章节，列出与主题相关的搜索结果链接。⚠️ 只使用搜索结果中提供的原始链接，严禁自行拼接、推测或编造任何 URL。如果搜索结果中没有提供链接，只写标题即可
6. 在"## 参考来源"章节之后，输出结构化参考文献 JSON，用 __REFS_START__ / __REFS_END__ 包裹，每条包含 title/author/year/url 字段，url 只用搜索结果原始链接或留空，严禁编造
7. 请生成修改后的完整报告，保持{format}格式"""
